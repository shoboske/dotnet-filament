<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <Description>Fila's in-app CLI: the make:panel, make:resource, make:page, make:widget, make:relation-manager and make:action scaffolding that the `dotnet fila` command dispatches into a target project. Install the Fila.Tools package to get that command.</Description>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Fila.Panels\Fila.Panels.csproj" />
  </ItemGroup>

</Project>
