<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <Description>Assertion helpers for testing a Fila panel, including assertions against Fila's htmx wire format. Deliberately tied to no test framework — it throws, so it fails a test under any runner.</Description>
  </PropertyGroup>

</Project>
