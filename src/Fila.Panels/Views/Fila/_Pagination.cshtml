@model FilaListViewModel
@{
    Layout = null;
    var url = $"/{Model.Panel.Path}/{Model.Resource.Slug}";
    var paginationModel = new PaginationNavModel(Model.Paged, url, "#fila-table", "#fila-table-state", PushUrl: true);
}
<partial name="Fila/_PaginationNav" model="paginationModel" />
